<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <!-- 边框 -->
    <stroke
        android:width="2dp"
        android:color="@color/blue_launchr"/>
    <!-- 圆角 -->
    <corners android:radius="1dp" />
    <!-- 边距 -->
    <padding
        android:bottom="5dp"
        android:left="13dp"
        android:right="13dp"
        android:top="5dp" />
    <solid android:color="@color/light_gray"/>
</shape>
